/**
 * Re-export from shared types for backwards compatibility.
 * Prefer importing directly from '#shared/types/chat' in new code.
 */
export type {
  User,
  UserProfile,
  Server,
  Channel,
  Message,
  FriendRelation,
  DmConversation,
  DmMessage
} from '#shared/types/chat'
